<Window x:Class="QuotaWatch.Tray.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Quota Watch"
        Width="440"
        SizeToContent="Height"
        MaxHeight="720"
        WindowStyle="None"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        Topmost="True"
        AllowsTransparency="True"
        Background="Transparent"
        ShowActivated="True"
        PreviewKeyDown="Window_OnPreviewKeyDown">
    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter" />
        <Style TargetType="TextBlock">
            <Setter Property="FontFamily" Value="Segoe UI" />
            <Setter Property="Foreground" Value="#FFE9EBF5" />
        </Style>
        <Style x:Key="HeaderButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#FF2A3042" />
            <Setter Property="Foreground" Value="#FFE9EBF5" />
            <Setter Property="BorderBrush" Value="#FF444C65" />
            <Setter Property="Padding" Value="12,6" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="1"
                                CornerRadius="6"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Border Background="#FF171A24"
            BorderBrush="#FF3A4158"
            BorderThickness="1"
            CornerRadius="12"
            Padding="18">
        <Border.Effect>
            <DropShadowEffect BlurRadius="22" Direction="270" ShadowDepth="6" Opacity="0.45" />
        </Border.Effect>
        <Grid x:Name="RootPanel" Focusable="True">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <Grid Grid.Row="0" Margin="0,0,0,14">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <StackPanel>
                    <TextBlock Text="Quota Watch" FontSize="20" FontWeight="SemiBold" />
                    <TextBlock Text="Claude + Codex" Foreground="#FF969DB5" FontSize="12" Margin="0,2,0,0" />
                </StackPanel>
                <Button Grid.Column="1"
                        Content="{Binding RefreshText}"
                        IsEnabled="{Binding CanRefresh}"
                        Style="{StaticResource HeaderButtonStyle}"
                        Click="RefreshButton_OnClick"
                        Margin="0,0,8,0" />
                <Button Grid.Column="2"
                        Content="×"
                        FontSize="17"
                        Style="{StaticResource HeaderButtonStyle}"
                        Click="CloseButton_OnClick"
                        Padding="10,4" />
            </Grid>

            <ScrollViewer Grid.Row="1"
                          MaxHeight="540"
                          VerticalScrollBarVisibility="Auto"
                          HorizontalScrollBarVisibility="Disabled">
                <ItemsControl ItemsSource="{Binding Providers}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border Background="#FF202534"
                                    BorderBrush="#FF32394D"
                                    BorderThickness="1"
                                    CornerRadius="9"
                                    Padding="14"
                                    Margin="0,0,0,10">
                                <StackPanel>
                                    <Grid Margin="0,0,0,10">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="Auto" />
                                            <ColumnDefinition Width="*" />
                                            <ColumnDefinition Width="Auto" />
                                        </Grid.ColumnDefinitions>
                                        <Ellipse Width="8" Height="8" Fill="{Binding StatusBrush}" VerticalAlignment="Center" />
                                        <TextBlock Grid.Column="1"
                                                   Text="{Binding Name}"
                                                   Foreground="#FFE9EBF5"
                                                   FontWeight="SemiBold"
                                                   FontSize="16"
                                                   Margin="8,0,0,0" />
                                        <TextBlock Grid.Column="2" Text="{Binding StatusLabel}" Foreground="{Binding StatusBrush}" FontSize="12" VerticalAlignment="Center" />
                                    </Grid>

                                    <ItemsControl ItemsSource="{Binding Buckets}">
                                        <ItemsControl.ItemTemplate>
                                            <DataTemplate>
                                                <StackPanel Margin="0,0,0,9">
                                                    <Grid Margin="0,0,0,5">
                                                        <Grid.ColumnDefinitions>
                                                            <ColumnDefinition Width="*" />
                                                            <ColumnDefinition Width="Auto" />
                                                        </Grid.ColumnDefinitions>
                                                        <TextBlock Text="{Binding Name}" Foreground="#FFD3D7E8" FontSize="13" />
                                                        <TextBlock Grid.Column="1" Text="{Binding PlanText}" Foreground="#FF7F88A4" FontSize="10" />
                                                    </Grid>
                                                    <ItemsControl ItemsSource="{Binding Windows}">
                                                        <ItemsControl.ItemTemplate>
                                                            <DataTemplate>
                                                                <Grid Margin="0,3,0,4">
                                                                    <Grid.ColumnDefinitions>
                                                                        <ColumnDefinition Width="32" />
                                                                        <ColumnDefinition Width="*" />
                                                                        <ColumnDefinition Width="58" />
                                                                    </Grid.ColumnDefinitions>
                                                                    <Grid.RowDefinitions>
                                                                        <RowDefinition Height="Auto" />
                                                                        <RowDefinition Height="Auto" />
                                                                    </Grid.RowDefinitions>
                                                                    <TextBlock Grid.Row="0"
                                                                               Text="{Binding Label}"
                                                                               Foreground="#FFAEB4C8"
                                                                               VerticalAlignment="Center" />
                                                                    <ProgressBar Grid.Row="0"
                                                                                 Grid.Column="1"
                                                                                 Value="{Binding UsedPercent}"
                                                                                 Maximum="100"
                                                                                 Height="8"
                                                                                 Margin="6,0,10,0"
                                                                                 VerticalAlignment="Center"
                                                                                 Foreground="{Binding ProgressBrush}"
                                                                                 Background="#FF353C50" />
                                                                    <TextBlock Grid.Row="1"
                                                                               Grid.Column="1"
                                                                               Text="{Binding ResetText}"
                                                                               Foreground="#FF858DA8"
                                                                               FontSize="10"
                                                                               Margin="6,-1,10,0" />
                                                                    <TextBlock Grid.Column="2"
                                                                               Grid.Row="0"
                                                                               Text="{Binding UsedText}"
                                                                               Foreground="{Binding ProgressBrush}"
                                                                               FontWeight="SemiBold"
                                                                               HorizontalAlignment="Right"
                                                                               VerticalAlignment="Center" />
                                                                </Grid>
                                                            </DataTemplate>
                                                        </ItemsControl.ItemTemplate>
                                                    </ItemsControl>
                                                </StackPanel>
                                            </DataTemplate>
                                        </ItemsControl.ItemTemplate>
                                    </ItemsControl>

                                    <TextBlock Text="{Binding Message}"
                                               Visibility="{Binding HasMessage, Converter={StaticResource BooleanToVisibilityConverter}}"
                                               Foreground="#FFD7BE78"
                                               TextWrapping="Wrap"
                                               FontSize="11"
                                               Margin="0,2,0,0" />
                                </StackPanel>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>

            <Grid Grid.Row="2" Margin="0,3,0,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <TextBlock Text="{Binding StatusText}" Foreground="#FF8F97B1" FontSize="11" TextTrimming="CharacterEllipsis" />
                <TextBlock Grid.Column="1" Text="{Binding LastUpdatedText}" Foreground="#FF8F97B1" FontSize="11" Margin="12,0,0,0" />
            </Grid>
        </Grid>
    </Border>
</Window>
